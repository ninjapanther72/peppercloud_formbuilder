import React, {useEffect} from 'react';

const FormInput = React.memo(({
                                     value,
                                 }) => {
    const TAG = "FormInput";

    useEffect(() => {

    }, []);

    return (<>
            <div
                className={``}
                style={{}}
            >
                {TAG}
            </div>
        </>
    )

    function fun() {

    }
});
export default FormInput;
